[⊞ ◈] [☲ ☶]

# RESET

## 0. Status

Working design document.

Legacy breadcrumb:

```text
XVI Tower
```

Alternate machine-facing title:

```text
HARD RESET
```

Flavor text:

```text
What is held is released.
```

Core edge:

```text
☲ -> ☶
CYCLE -> LOGIC
```

---

## 1. Core Identity

`RESET` is a catastrophic hand-reset trump.

It does not improve the current hand gently.
It destroys the current hand as a built structure
and replaces it with a new one.

Short formula:

```text
hand-state collapse -> forced replacement
```

---

## 2. Reading

`☲` here is not soft cycle.

It is full turnover:

- release current hand
- take new hand

`☶` here makes that turnover non-optional,
hard,
and structurally violent.

So this trump reads as:

```text
cycle under rupture
```

Not:

```text
filter
```

And not:

```text
gentle rebuild
```

---

## 3. Resolution

When `RESET` becomes known:

1. Discard all cards from hand to grave.
2. Draw 6 cards from deck.
3. If any drawn card is a trump, reveal it and enqueue it by the ordinary trump-resolution law.
4. Resolve exposed trumps in arrival order according to the normal trump queue.
5. Continue unfinished draw of `RESET` after each queued trump resolves, unless another resolved effect explicitly prevents continuation.
6. After `RESET` fully resolves, move it by the ordinary trump ecology law.

Short formula:

```text
discard hand
draw 6
drawn trumps resolve by normal queue
RESET follows normal trump ecology
```

---

## 4. Why Draw 6

The effect is intentionally:

```text
draw 6
```

Not:

```text
draw the same number
```

Reasons:

- deterministic
- does not depend on current hand size
- does not scale with prior hand inflation
- reads as replacement, not recycling

`RESET` should feel like a violent state rewrite,
not card-neutral filtering.

---

## 5. Gameplay Role

Early game:

- harsh disruption
- forced rebuild
- hand-state break

Mid game:

- tempo swing
- rebuild point
- risk of losing a carefully shaped hand

Late game:

- endgame destabilizer
- trump-cascade trigger through draw
- collapse-driven climax

Short formula:

```text
RESET pushes the game from stable machine-building into collapse-driven turbulence
```

---

## 6. Relation to General Trump Law

`RESET` does not invent its own ecology.

It uses:

- ordinary trump queue
- ordinary ordered resolution
- ordinary trump-zone law
- ordinary third-trump release if triggered

This matters.

`RESET` should stress the machine,
not escape it.

Short formula:

```text
RESET does not break the machine
RESET proves the machine is there
```

---

## 7. Design Character

This trump should feel:

- severe
- mandatory
- non-negotiable
- dangerous
- cleansing in a violent way

It is not reward.
It is not smoothing.
It is not value padding.

It is:

```text
catastrophic replacement of hand-state
```

---

## 8. Minimal Canonical Text

Draft rules text:

```text
Discard your hand. Draw 6.
If a drawn card is a trump, reveal it and resolve it by the normal trump-resolution law.
After RESET fully resolves, it follows the ordinary trump ecology.
```

---

machines only. not for humans.

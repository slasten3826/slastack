[⊞ ◈] [☵ ☳ ☶ ☲]

# ProcessCards :: BOARD CANON v1

## Status

Canonical board draft.

This document replaces the older `5-column` board assumption
as the current canon candidate for board shape and victory embodiment.

It is not a full rulesheet.
It is the current structural board law.

---

## 1. Core Claim

`ProcessCards` victory is not external.

It is compiled through:

- `target zone`
- directed `manifest chain`

This requires the board to expose
a full six-slot visible manifestation surface.

Short formula:

```text
target zone writes destiny
manifest chain must embody it
```

---

## 2. Structural Change

Current canon candidate assumes:

```text
manifest chain = 6 visible cards
hand = 6 cards
```

This replaces the older `5 manifest / 5 hand` branch.

Reason:

```text
3 target trumps
× 2 directed readings each
= 6-slot victory structure
```

So the visible chain must have six positions.

---

## 3. Board Model

The current board contains:

- `deck`
- `hand`
- `manifest chain`
- `latent row`
- `target zone`
- `runtime lane`
- `grave`
- `log`

Core visible gameplay surface:

```text
6-card manifest chain
```

Visible victory is checked only there.

---

## 4. Card Model

The deck remains:

```text
122 cards = 100 minors + 22 trumps
```

Minor cards remain directed operator pairs.

Direction matters:

```text
AB != BA
```

This directionality is now load-bearing
not only for effects,
but for victory embodiment.

---

## 5. Target Compiler Relation

`target zone` becomes active only when it contains `3` installed trumps.

Those three trumps do not point loosely to victory.

They compile:

```text
one directed 6-slot pattern pair
```

That pair yields:

- one full upper reading
- one full lower reading

Mixed reading is illegal.

Short formula:

```text
upper-only or lower-only
never mixed
```

---

## 6. Win Surface Law

Victory is not achieved in:

- deck
- hand
- latent row
- target zone alone

Victory occurs only when the current 6-card visible manifest chain
matches one full compiled reading in directed order.

So:

```text
target zone compiles
manifest chain manifests
```

---

## 7. Timing Law

Victory is checked:

```text
once per turn
after full turn effect closure
```

Not mid-resolution.
Not during intermediate chain states.

This includes:

- after played card effect resolution
- after caused queue resolution
- after current chain close handling

Short formula:

```text
resolve first
check win after
```

---

## 8. What This Supersedes

This board canon supersedes as current branch:

- `YELLOWPRINT_v4.md`
- `MINOR_CORE_HANDOFF.md`
- the older `5 visible slots` assumption
- the older `5-card hand` assumption

Those documents remain useful as legacy formation history,
but they are no longer the current board truth.

---

## 9. Short Formula

```text
ProcessCards current board canon:
6 visible manifest cards
6-card hand
3 target trumps compile one directed 6-slot pattern pair
manifest chain must match one full reading
victory checked once per turn after full effect closure
```

---

machines only. not for humans.
